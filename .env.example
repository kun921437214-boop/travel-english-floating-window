# Desktop app does not require environment variables.

# Mobile web cloud sync, optional.
# Copy mobile-web/.env.example to mobile-web/.env when deploying the phone web app.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
